# ktc4-team-21

*지금 이 문서는 프로젝트 설명보다 **협업 규칙**을 우선으로 정리했다. 백엔드에 두 번째 담당자가 붙고 프론트도 곧 시작되는 지금 단계에서 가장 필요한 게 "누가 뭘 보고 어떻게 움직이는가"라서다. 제품 내용(문제 정의·화면·지표·일정 등)은 최소한만 남기고 `최종기획안.md`로 넘겼다. 팀이 자리 잡히면 이 README도 프로젝트 소개 중심으로 다시 정리한다.*

## 이 서비스가 뭔지 (최소 요약)

친구 3~5명이 하나의 지도에 핀을 찍어 여행 장소를 정하고, 의견이 갈리면 AI가 반대 사유를 반영한 대안을 제시해 합의에 이르는 서비스. 제품 스펙 전체(문제 정의·화면 설계·성공 지표·일정)는 `최종기획안.md`가 정본이다.

---

## 저장소 구조 — 문서가 곧 협업 방식이다

```
/CLAUDE.md              모든 사람·모든 Claude Code가 공유하는 규칙
                         (가드레일 10개, 용어 고정, v1/v2 스코프, 이슈 운영 원칙)
/최종기획안.md            제품 스펙 정본
/docs/                  기술 계약 정본 — api-spec.yaml(OpenAPI), data-model.md(DB 스키마),
                         constraints.md(실격 조건), permissions.md(권한), events.md(SSE),
                         errors.md(에러 코드), code-quality.md(리뷰 기준) 등
/contracts/             프론트가 백엔드 완성을 기다리지 않게 하는 패키지
                         (docs/api-spec.yaml에서 타입 생성 + 실제로 동작하는 목 서버)
/backend/CLAUDE.md       백엔드 공통 컨벤션 (단일 FastAPI 앱, 모듈 간 접근 규칙, 마이그레이션)
/backend/<module>/       모듈별 구현 코드 + 그 모듈만의 CLAUDE.md
/frontend/CLAUDE.md      프론트 공통 컨벤션 (스택, 화면 구조, 계약 사용법)
```

문서가 3단(루트 → backend/frontend 공통 → 모듈별)으로 나뉜 이유는 단순하다 — **각자 자기 몫만 보고 바로 착수할 수 있어야 한다.** 다른 사람이 뭘 하는지 알고 싶으면 위 단계로 올라가면 된다. 프로젝트 설명과 협업 규칙이 각각 다른 문서에 있는 것도 같은 이유다 — README 하나에 다 몰아넣으면 결국 아무도 안 읽는다.

---

## 어떻게 시작하는가

### 공통 — 프론트든 백엔드든 처음엔 이걸 먼저
1. `/CLAUDE.md`를 읽는다.
2. GitHub 이슈에서 자기 라벨(`fe`/`be`/`ai`/`qa`)이 붙고 담당자 없는 걸 고른다. 이슈 본문 끝의 `복잡도: N/5`를 참고한다.
3. `decision` 라벨이 붙은 이슈는 아직 팀이 결정하지 않은 것들이다. 맡으려는 작업이 거기 걸려 있으면 결정부터 먼저 확인한다.

### 프론트엔드 담당자 (장현성)
- `frontend/CLAUDE.md`부터 읽는다 — 스택, 화면 구조(탭 3개: 지도/AI추천/리스트), 디렉토리 컨벤션이 있다.
- **백엔드를 기다리지 않는다.** `contracts/`가 타입 + 실제로 상태가 바뀌는 목 서버(msw)를 준다. `contracts/README.md`대로 세팅하면 실제 API 없이 전 화면을 만들 수 있다.
- `docs/api-spec.yaml`이 계약 정본이다. 여기서 스펙을 바꾸지 않는다 — 부족하거나 틀렸다고 판단되면 루트에 보고한다. 스펙이 바뀌면 `docs/CHANGELOG-api.md`에 먼저 기록된다.

### 백엔드 담당자 (김민재, 김도윤)
- `backend/CLAUDE.md`부터 읽는다 — 단일 FastAPI 앱 구조, 모듈 간 import 금지, 마이그레이션 체인은 하나라는 걸 명시해뒀다. **두 사람이 서로 다른 모듈에서 동시에 시작해도 같은 가정으로 합쳐지게 하기 위한 문서다.**
- 맡을 모듈(`backend/<module>/`)을 정했으면, **그 디렉토리를 새 터미널로 열고 거기서 별도 Claude Code 세션을 새로 시작한다.** 그 모듈의 `CLAUDE.md`가 책임·경계·완료 정의를 갖고 있다.
- 다른 모듈의 테이블을 직접 쿼리하지 않는다 — 필요하면 그 모듈이 공개한 함수나 API로.
- 스펙이 부족하거나 틀렸다고 판단되면 직접 고치지 말고 루트에 보고한다.

### 루트 (아키텍처 담당)
- `docs/`·`contracts/`처럼 여러 쪽이 공유하는 계약·툴링만 직접 만든다. 모듈 구현은 하지 않는다 — 담당 디렉토리와 이슈 번호만 안내하고, 그 사람이 새 세션을 연다.
- 새 작업 착수 전 이슈 번호를 먼저 밝히고, 프론트에 그대로 전달 가능한 명세 요약을 함께 낸다.

---

## 협업이 실제로 돌아가는 방식

- **누구도 서로를 기다리지 않는다.** 계약(`docs/api-spec.yaml`)이 확정되면 프론트는 목 서버로, 백엔드는 그 스펙대로 각자 개발하고 나중에 API만 갈아끼운다.
- **코어가 먼저, 이슈는 나중에 쪼갠다.** 계약·스키마가 안 굳었으면 이슈를 잘게 나누지 않는다 — 너무 일찍 나누면 그 자체가 병목이다.
- **AI 품질보다 FE+BE 코어 루프가 먼저다.** `backend/llm`은 고정 응답 스텁으로 시작해도 된다. 처음부터 완성형으로 가지 않는다.
- **이슈마다 예상 복잡도(1~5)를 적고, 끝나면 실제 소요를 적는다.** 회고 때 "이런 유형은 이 정도 걸린다"가 나오게 하기 위함이다.
- **스펙 변경은 즉시 전파된다.** 구조가 바뀌면 `docs/CHANGELOG-api.md`에 먼저 기록되고 관련자에게 바로 보고된다 — 각자 알아서 알아채는 게 아니다.
- **새 문서를 만들기 전엔 `/CLAUDE.md`의 문서 지도부터 본다.** 이미 있는 문서에 절을 추가하는 게 기본이다 — 문서가 여러 곳에 같은 내용을 담으면 그중 하나가 갱신에서 빠지고 어긋난다(실제로 한 번 겪었다: 이 저장소와 `최종기획안.md`가 서로 다른 시점에 갱신되며 지도 API·화면 구조가 어긋났었다).

---

## 코드 리뷰 기준

AI가 코드 대부분을 작성하므로 스타일 리뷰는 의미가 적다. 사람은 아래를 본다.

- 요청받은 범위만 건드렸는가
- API 스펙과 실제 응답이 일치하는가
- 실격 조건·가드레일 같은 도메인 규칙을 지켰는가
- 테스트가 진짜 실패 케이스를 잡는가, 아니면 통과만 하도록 짜였는가
- 에러를 조용히 삼키지 않는가
- PR 설명에 "왜 이렇게 했는지"가 있는가

세부 클린 코드 원칙은 `docs/code-quality.md` 참고.

---

## 팀

| 역할 | 담당 |
|---|---|
| PM · 최종 의사결정 | 권효정, 황준영 |
| 프론트엔드 | 장현성 |
| 백엔드 · API 연동 | 김민재, 김도윤 |
| AI 프롬프트 · 평가셋 | 김관호 |
| QA · 평가 | 김도윤 |
| 통합 | 표강준 |

정렬 미팅: 매주 화 13:00 온라인

---

## 더 읽을 것

문제 정의·타깃·화면 설계·가드레일·기술 스택·일정·성공 지표·미결 사항 같은 프로젝트 상세는 `최종기획안.md`와 GitHub 이슈에 있다. 이 README는 "어디서 뭘 보고 어떻게 움직이는지"만 담당한다 — 프로젝트 내용을 여기 다시 옮겨 적지 않는 것도 위 "협업이 돌아가는 방식" 마지막 항목(문서 중복 금지)과 같은 이유다.
