# ktc4-team-21
카카오테크 캠퍼스 4기 2단계 팀 프로젝트 — 충남대 2팀

## 여행 공동 지도

친구 3~5명이 하나의 지도에 핀을 찍어 여행 장소를 정하고 의견이 갈리면 반대 사유를 반영한 대안 핀을 받아 합의에 이르는 서비스.

### 왜 만드나

장소를 모으는 일 자체는 지도로 이미 해결되지만 누가 "여기 별로야"라고 말한 뒤 아무도 다음 후보를 내지 않아 결정이 멈추고, 멈춘 대화는 잡담에 묻혀 며칠 뒤 "그래서 어디로 정했지"가 반복된다. AI는 이 병목 하나에만 쓴다.

### 타깃

정해진 리더 없이 3~5명이 함께 국내 여행을 계획하는 팀. 주말 여행, 동아리 MT, 연휴 단기 여행, 가족 여행 모두 해당한다. 
전제는 셋뿐이다 
— 3~5명이 함께 정한다, 국내 여행이다, 카톡으로 장소를 공유해왔다.

### 핵심 설계 원칙

제약을 두 종류로 나눈다. 나머지 설계가 전부 여기서 파생된다.

| | 실격 (hard) | 선호 (soft) |
|---|---|---|
| 무엇 | 못 먹는 재료·알러지, 수용 인원, 예산 상한, 반경, 영업 여부 | 분위기, 한적함, 지역적 특색 |
| 누가 판정 | 코드가 후보에서 제거 | 모델이 통과분의 순위에만 반영 |
| 틀렸을 때 | 싫다고 말한 것을 추천하는 사고 | 취향이 안 맞는 정도 |
| UI 표기 | 「꼭 지켜야 해요」 | 「있으면 좋아요」 |

### 동작 흐름

```
핀 찍기 (카카오맵 링크 붙여넣기 / 이름 검색 / 길게 눌러 찍기)
  → 핀마다 ♥ 좋음 / △ 조율 필요 / 🚫 반대 + 사유
  → 카테고리별로 의견 남긴 핀이 구성원 절반 이상이면 추천 가능
  → 근거 확인 화면에서 AI가 정리한 조건을 사람이 검토·수정
  → 코드가 실격 필터로 후보를 거르고, 모델이 통과분의 순위만 매김
  → 대안 3곳이 점선 핀으로 지도에 뜸 (요청한 사람에게만)
  → 「지도에 올리기」를 눌러야 다른 구성원에게도 보임
  → 리스트 탭에서 드래그해 확정 리스트에 넣고 순서 정함
```

요청마다 반드시 도는 모델 호출은 사유 구조화와 선호 순위 두 곳이다. 후보가 캐시에 없을 때만 장소 라벨링이 추가로 붙는다. 나머지는 코드가 처리한다.

### 화면

탭 세 개다.

| 탭 | 내용 |
|---|---|
| 지도 (기본) | 분류·종류·의견 필터 + 전체 지도. 핀 종류는 색이 아니라 모양으로 구분한다(확정=채운 핀, 일반=실선, AI 추천=점선). 색은 누가 찍었는지를 나타내되, 확정 핀과 AI 추천 핀은 고정색을 써서 제안자 색을 버린다 |
| AI 추천 | 지도가 없다. 근거 확인 화면 → 결과 카드 3곳. 위치는 지도 탭에서 점선 핀으로 본다 |
| 리스트 | 왼쪽 핀 목록, 오른쪽 최종 코스. 오른쪽으로 드래그하면 그 자체가 확정이고 드롭한 자리가 방문 순서 |

### 가드레일

- AI는 지도를 바꾸지 않는다. 대안은 요청한 사람에게만 먼저 보이고 그 사람이 올려야 팀에 보인다.
- 사유 없는 반대는 등록되지 않는다.
- 제약을 AI가 임의로 완화하지 않는다. 반경을 넓히는 것도 사람이 한다.
- 결과가 0곳이면 억지로 채우지 않고 0곳으로 표시한다. 조건을 푸는 건 사람의 몫이다.
- 재료는 대표 메뉴로 판정하고 불확실하면 "메뉴 확인 필요"로 표시하며 확정하지 않는다.
- 한 명이라도 실격이면 후보에서 내린다.

### 기술 스택

| 구분 | 선택 |
|---|---|
| 프론트엔드 | Vite + React + TypeScript, Tailwind + shadcn/ui |
| 백엔드 | FastAPI + Pydantic V2 + SQLAlchemy |
| DB | PostgreSQL 17 + PostGIS |
| 지도 | 카카오맵 (JS SDK + 로컬 API) |
| 로그인 | 카카오 소셜로그인 |
| 배포 | Cloud Run + Cloud SQL |
| AI API | 미정 — 아래 참조 |

지도 SDK는 카카오맵으로 확정했지만, **카카오 로컬 API는 가격·평점·영업시간을 주지 않는다.** 상호명·카테고리·주소·좌표·전화번호가 전부다. 돈을 더 내면 주는 상위 등급이 있는 게 아니라 스키마 자체에 그 필드가 없다. 실격 조건 중 상당수가 판정할 근거 데이터를 못 구하는 상태라, 소스를 하나로 두지 않고 자체 DB에 여러 소스(카카오, 구글, 사용자 입력)를 추상화하는 계층을 두는 방향으로 잡고 있다.

PostGIS는 그대로 간다. 반경 검색과 동선 계산이 서비스의 핵심이라 지리 공간 인덱스가 필요하다. 겹치는 원을 걸러낼 때는 `&&` intersect 연산자를 쓴다.

지도 SDK가 명령형이고 공식 React 래퍼가 없어서 마커 상태는 React 바깥에서 독립적으로 관리하고 React는 UI 상태만 갖는 구조로 간다. 상태 관리 라이브러리는 데이터 흐름을 먼저 그린 뒤에 정한다.

### 일정

8/26 ~ 11/14, 약 11.5주. 목표는 완성이 아니라 배포 후 실사용 기반 개선이다. 관찰→고침→다시 관찰이 최소 네 번은 돌아야 하고 여기서 4주와 안정화·발표 준비 2주를 빼면 9월 말이 배포 마지노선이다.

| 기간 | 단계 | 끝났다고 보는 기준 |
|---|---|---|
| 8/26–8/31 | 준비 | 커밋하면 자동 배포되는 URL이 있다 |
| 9/1–9/21 | v1 구현 | 팀이 실제 여행 하나를 이 도구로 계획해봤다 |
| 9/22–9/30 | 배포·첫 사용자 | 우리가 아닌 사람이 만든 지도가 끝까지 굴러갔다 |
| 10/1–10/26 | 실사용·개선 | 개선 사이클이 네 번 돌았다 |
| 10/27–11/9 | 안정화 | 시연이 안 깨진다 |
| 11/10–11/14 | 발표 준비 | |

**v1 (9월)** — 지도·핀 찍기, 반응과 사유, 실격 필터, 검색 범위 계산, 근거 확인 화면, AI 추천과 「지도에 올리기」, 확정 리스트, 실시간 동기화

**v2 (10월 이후)** — 카톡 일괄 파싱, AI 동선 짜주기, 구성원 강퇴, 개인화 코멘트, 조건 완화 시뮬레이션, 근거 부족 화면, 서비스 피드백

### 정해야 할 것

위쪽일수록 다른 작업을 막고 있다.

| # | 항목 | 상태 |
|---|---|---|
| 1 | 가격·영업시간 실격 조건 처리 | 카카오 API가 안 준다. 실격→선호로 낮출지, 사용자 태깅으로 채울지, v1에서 뺄지 |
| 2 | 선호 순위 점수 기준과 태그 사전 | 정의된 게 없다. "맛있다"를 누가 말했든 같은 태그로 통일할 사전이 필요 |
| 3 | AI 모델 확정 | 저비용 모델로 시작해 필요하면 올리는 방향. 시작 모델과 재평가 시점 미정 |
| 4 | 장소 데이터 추상화 계층 설계 | 자체 DB 스키마, 소스별 우선순위, 영구 저장 범위 |
| 5 | 제안·거절 이력의 단위 | 추천은 개인이 실행하는데 이력이 지도 단위면 취향이 섞인다 |
| 6 | 웹소켓 유지 여부 | 이벤트 빈도가 낮아 SSE로 충분할 수 있다 |
| 7 | jsonb 필드의 용도 | 추천 로직 입력인지 재처리용 원본 보존인지 필드별 구분 필요 |
| 8 | AI 추천 결과의 상태 관리 위치 | 서버에서 왔지만 개인용·휘발성이다 |
| 9 | 장소 라벨링 시점과 정정 방법 | 온디맨드 vs 사전 채움, 신뢰도 저장·사용자 신고 |

프론트엔드 설계서의 자잘한 미정 항목 20여 개는 그 문서에 남겨두고 위 9개가 정리되는 대로 처리한다.

### 지표

| 구분 | 지표 | 목표 |
|---|---|---|
| 최우선 | 실격 조건 위반율 | 0% (사전 실측 40%) |
| 핵심 | 조율 필요 → 확정 전환율 | 10건 중 6건 |
| 보조 | 대안 게시율 / 채택률 | 50% / 60% 이상 |
| 보조 | 24시간 내 결정 비율 | 70% 이상 |
| 보조 | 진입 전환율 | 70% 이상 |
| 보조 | 반대 대비 사유 기록률 | 80% 이상 |
| 보조 | 같은 장소 반복 추천 비율 | 20% 이하 |

### 코드 리뷰 기준

AI가 코드 대부분을 작성하므로 스타일 리뷰는 의미가 적다. 사람은 아래를 본다.

- 요청받은 범위만 건드렸는가
- API 스펙과 실제 응답이 일치하는가
- 실격 조건·가드레일 같은 도메인 규칙을 지켰는가
- 테스트가 진짜 실패 케이스를 잡는가, 아니면 통과만 하도록 짜였는가
- 에러를 조용히 삼키지 않는가
- PR 설명에 "왜 이렇게 했는지"가 있는가

### 팀

| 역할 | 담당 |
|---|---|
| PM · 최종 의사결정 | 권효정, 황준영 |
| 프론트엔드 | 장현성 |
| 백엔드 · API 연동 | 김민재, 김도윤 |
| AI 프롬프트 · 평가셋 | 김관호 |
| QA · 평가 | 김도윤 |
| 통합 | 표강준 |

정렬 미팅: 매주 화 13:00 온라인

### 백엔드 온보딩 — 김도윤

이 저장소엔 이미 아키텍처 설계·API 계약·모듈별 작업 지침이 들어 있다. 클론해서 아래 순서로 시작하면 지금까지의 논의를 다시 거치지 않고 바로 이어받을 수 있다.

1. **`/CLAUDE.md`부터 읽는다.** 역할 분리(루트 vs `backend/<module>/`), 가드레일 10개, "모델 호출은 정확히 3곳뿐"이라는 원칙, 용어 고정, v1/v2 스코프 경계가 여기 있다.
2. **맡을 이슈를 고른다.** GitHub 이슈에서 `be` 라벨 + 담당자 없는 것 중 고른다. 각 이슈 본문 끝에 `복잡도: N/5`가 적혀 있으니 참고한다.
3. **그 이슈가 `backend/<module>/` 소속이면(대부분 그렇다) 이 저장소를 다시 새 터미널로 그 디렉토리에서 열고, 거기서 별도 Claude Code 세션을 새로 시작한다.** 예: 핀 관련 이슈면 `backend/pins/`에서 세션을 열고 그 폴더의 `CLAUDE.md`를 먼저 읽게 한다 — 그 모듈의 책임·경계·완료 정의가 이미 정리돼 있다. 루트는 이 구현을 대신 해주지 않는다(`/CLAUDE.md`의 "작업 착수 프로토콜" 참고).
4. **API 계약은 `docs/api-spec.yaml`이 정본이다.** 프론트는 이미 `contracts/`의 목 서버(msw)로 이 계약에 맞춰 개발 중이니, 스펙과 다르게 구현하면 프론트가 깨진다. 스펙이 부족하거나 틀렸다 싶으면 직접 고치지 말고 루트 담당(김민재)에게 알린다 — 바뀌면 `docs/CHANGELOG-api.md`에 기록된다.
5. **착수 전 이슈에 복잡도(1~5)를 적었는지 확인하고, 끝나면 실제 소요를 적는다.**

막히면 `/CLAUDE.md` 맨 아래 "문서 지도"에서 관련 문서부터 찾아본다.
