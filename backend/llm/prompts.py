"""backend/llm 모델 호출 2곳의 프롬프트 본문.

모델 확정(#12) 이후 실제 프롬프트로 채우는 자리(backend/llm/CLAUDE.md "우선순위" 절,
PR #44 리뷰 반영). service.py의 각 함수가 모델을 실제로 호출하게 되면 여기 상수를
그대로 시스템/유저 프롬프트로 사용한다.

② plan_evidence와 ③-a-1 label_place 프롬프트가 들어올 자리다. 둘 다 공통 목표가
같다 — 사람이 쓴 자유 텍스트와 장소 원자료를 `docs/constraints.md`의 고정된
`fact_key`로 정규화하는 것. 그 뒤 단계(실격 필터·선호 순위)는 전부 코드가 맡으므로,
여기서 나오는 `fact_key`의 일관성이 추천 품질을 사실상 결정한다.

③-b 선호 순위 프롬프트(RANK_CANDIDATES_PROMPT)는 2026-09-21에 제거했다(#99).
거기 박혀 있던 배점(quiet 3점, 나머지 2점, 여러 명 언급 시 최대 3배)은 근거를 댈 수
없는 상수였고, 규칙이 전부 산술이라 모델이 판단할 여지가 없었다. 지금 규칙은
docs/constraints.md "선호 점수 계산 (③-b)"에 있고 recommend가 코드로 구현한다.
"""
