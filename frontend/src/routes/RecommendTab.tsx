export default function RecommendTab() {
  return (
    <div className="p-4">
      <h2 className="mb-2 text-lg font-semibold">AI 추천</h2>
      {/* 준비 판정(#11)·근거 확인(#12)·결과와 지도에 올리기(#14)가 여기로 들어온다. */}
      <p className="text-sm text-muted-foreground">
        추천 준비 판정과 대안 핀이 여기에 들어옵니다
      </p>
    </div>
  )
}
