# pingo — 프로젝트 규칙

이 문서는 프로젝트에 참여하는 모든 사람과 모든 Claude Code(루트·서브 디렉토리)가 공통으로 따르는 규칙이다.
제품 스펙의 정본은 `최종기획안.md`이고, 기술 계약의 정본은 `docs/`다. 둘이 충돌하면 `docs/`가 최신이고, `최종기획안.md`는 팀 보고 후 갱신한다(각 문서 상단 변경이력 참고).

## 역할 분리 — 오버스테핑 금지

```
루트 (이 저장소 최상위)
  ├─ 아키텍처 설계, 모듈 간 API 스펙 정의, 이슈/작업 분할, 통합 테스트
  └─ FE↔BE 데이터 구조 정의 — 구조가 바뀌면 즉시 docs/CHANGELOG-api.md에 기록하고 각 담당자에게 보고

backend/ (backend/<module>/마다 별도 Claude Code)
  └─ backend/CLAUDE.md(백엔드 공통) + backend/<module>/CLAUDE.md(모듈별)를 따른다
  └─ 루트가 정의한 스펙(docs/api-spec.yaml, docs/data-model.md, docs/constraints.md)에 맞춘 상세 구현
  └─ 해당 모듈의 단위 테스트 작성·통과까지가 책임의 끝
  └─ 스펙이 부족하거나 틀렸다고 판단되면 스펙을 임의로 바꾸지 말고 루트에 보고한다

frontend/ (서브 디렉토리, 별도 Claude Code)
  └─ frontend/CLAUDE.md를 따른다
  └─ 위와 동일 원칙 — 루트 계약(docs/api-spec.yaml 등)을 임의로 바꾸지 않는다
```

서브 디렉토리 Claude Code는 자기 쪽 `CLAUDE.md`를 반드시 먼저 읽는다. 거기 없는 결정(다른 모듈·사이드와의 계약, DB 스키마 변경, 실격 조건 값, 화면 구조)은 루트 소관이다.

## 작업 착수 프로토콜

프론트 담당자가 백엔드 진도에 발맞춰 병렬로 움직이는 게 이 팀의 기본 개발 양상이다. 그래서 루트 Claude Code는 새 작업에 들어갈 때마다 다음을 지킨다.

1. **착수 전, 진행할 GitHub 이슈 번호를 먼저 보고한다.** 어떤 작업인지 번호로 먼저 밝히고 시작한다.
2. **`backend/<module>/`·`frontend/` 구현이 필요한 작업은 루트가 직접 하지 않는다.** 담당 디렉토리와 이슈 번호만 안내한다 — 사용자가 그 디렉토리에서 별도 터미널·별도 Claude Code 세션을 새로 연다. 루트가 직접 만드는 건 `docs/`·`contracts/`처럼 여러 모듈·사이드가 공유하는 계약·툴링뿐이다.
3. **착수 직후, 프론트 담당자에게 그대로 전달 가능한 요약을 함께 낸다.** "무엇을 개발하는지 + 명세(엔드포인트·스키마·연결 방법)"를 백엔드 리더가 복붙해서 보낼 수 있는 형태로 정리한다.
4. 구조가 바뀌면 `docs/CHANGELOG-api.md`에 먼저 기록하고 즉시 보고한다(기존 규칙, 위와 같은 이유 — 프론트 병목 방지).

## 이슈 운영 원칙

- **모든 개발 이슈에 예상 복잡도(1~5)를 적는다.** 끝나면 실제 소요도 적는다 — 회고에서 "이런 유형은 이 정도 걸린다"가 나오게 하기 위함이다. 결정 이슈(`decision` 라벨)는 대상이 아니다.
- **코어(계약·스키마)가 안정되기 전엔 이슈를 잘게 쪼개지 않는다.** 코어는 페어로 같이 잡고, 살 붙이는 단계부터 이슈를 나눈다 — 너무 일찍 쪼개면 그 자체가 병목이 된다.
- **AI(LLM 실제 응답) 없이도 FE+BE 코어 루프가 도는 데모가 우선이다.** `backend/llm`은 초기엔 고정 응답 스텁으로 두고, `backend/recommend`는 그 스텁에 맞춰 통합부터 끝낸다. AI 품질은 스텁이 자리 잡은 뒤 아랫단계부터 끌어올린다 — 처음부터 완성형으로 가지 않는다.
- **새 문서를 만들기 전에 아래 문서 지도부터 확인한다.** 이미 있는 문서에 절을 추가하는 게 기본이고, 새 파일은 정말 새 범주일 때만 만든다(이번에 `docs/code-quality.md` 하나만 추가한 이유).

## 절대 원칙 (기획안 7절 가드레일 요약 — 원문은 기획안 참고)

1. AI는 지도를 바꾸지 않는다. 대안은 요청한 사람에게만 먼저 보이고, 「지도에 올리기」로만 공개된다.
2. 조회되지 않는 장소는 핀으로 만들지 않는다. 결과 0개는 0개로 보여준다.
3. 사유 없는 반대는 등록되지 않는다.
4. 제약을 AI가 임의로 완화하지 않는다. 반경을 넓히는 것은 사람이 한다.
5. 대안 핀에는 이유·조건별 충족 체크·구성원 충족 집계·출처가 항상 붙는다. 게시된 뒤에도 유지된다.
6. 이미 제안했거나 거절된 장소는 후보에서 제외한다.
7. 실격 조건은 코드가 거른다. 모델은 통과분의 순위만 매긴다.
8. 판정 불확실("모름")은 조건 종류에 따라 다르게 처리한다 — `docs/constraints.md`의 `unknown_policy` 참고. 안전 조건은 절대 통과시키지 않는다.
9. 한 명이라도 실격이면 후보에서 내린다.
10. 충돌 선언은 최후에만. 무엇을 시도했는지 화면에 남긴다.

## 모델 호출은 3곳뿐

② 사유 → 실격/선호/반경 구조화, ③-a-1 장소 라벨링(캐시 미스 시만), ③-b 선호 순위. 이 3곳 외에 모델을 추가로 부르는 기능(예: 동선 계산, 카톡 대화 해석)은 기획안이 명시적으로 금지한다. 새 기능을 추가할 때 먼저 "이거 코드로 되나?"를 묻는다 — 값이 있는 조건이면 답은 거의 항상 "된다"(기획안 8절).

## 용어 고정 (기획안 9절)

반응(♥ 좋음/△ 조율 필요/🚫 반대/? 미확인) · 핀 종류(확정/일반/AI 추천, 이 3개뿐) · 「지도에 올리기」 · 카테고리(음식점/카페/숙소/관광지) · 제약(실격/선호) · 확정 리스트.
쓰지 않는 말: 장소 카드·보드·칸반·여행방. 장소는 언제나 핀, 공간은 언제나 지도.

## 스코프 경계 (기획안 11절, `docs/` 확정 사항 반영)

- **v1 범위 안**: 공동 지도·초대·소셜 로그인·구글맵/카카오맵 링크 파싱·핀 반응/사유·실격 필터·대안 추천·확정 리스트·확정 리스트 동선 보기(순서만, 시간 배정 없음)
- **v2로 이동**: 카톡 대화 내보내기 일괄 파싱(5-9), 구성원 강퇴, 서비스 피드백
- **범위 밖 (요청받아도 만들지 않는다)**: 항공·숙박 예약·경비 정산·자체 채팅·SNS·전체 일정 자동생성·실시간 위치공유·환율·날씨·AI의 최종 장소 결정·인스타/블로그 링크 파싱
- 동선 "순서"는 범위 안이지만 "몇 시에 어디"라는 시간 배정은 전체 일정 자동생성에 해당해 범위 밖이다. **동선 계산은 모델을 쓰지 않는다** — 최근접 이웃 기반 순수 계산.

## 문서 지도

| 문서 | 용도 |
|---|---|
| `최종기획안.md` | 제품 스펙 정본(변경 이력 포함) |
| `docs/architecture.md` | 모듈 경계, 데이터 3층 모델, 프리시딩 흐름 |
| `docs/data-model.md` | DB 스키마 |
| `docs/constraints.md` | 실격/선호 조건 레지스트리, `unknown_policy` — **값 변경은 루트만** |
| `docs/permissions.md` | 롤 → 범위+액션 권한 모델 |
| `docs/api-spec.yaml` | OpenAPI 3.1 — FE↔BE 계약의 정본 |
| `docs/events.md` | SSE 이벤트 스키마 |
| `docs/errors.md` | 에러 코드 카탈로그 |
| `docs/openapi-workflow.md` | 목 서버·타입 생성·스펙 변경 통보 절차 |
| `docs/CHANGELOG-api.md` | 스펙 변경 이력 — 구조 변경 시 여기 먼저 기록 |
| `docs/code-quality.md` | 클린 코드 원칙 + AI 작성 코드를 사람이 무엇을 리뷰할지 |
| `contracts/` | FE 타입 생성 + 목 서버(msw) 패키지. `docs/api-spec.yaml`에서 파생됨. `contracts/README.md` 참고 |
| `backend/CLAUDE.md` | 백엔드 공통 컨벤션 — 단일 FastAPI 앱 구조, 마이그레이션, 모듈 간 접근 규칙 |
| `frontend/CLAUDE.md` | 프론트 공통 컨벤션 — 스택, 화면 구조, 계약 사용법 |
| `docs/execution-order.md` | 이슈 "선행:" 표기 기반 실행 순서 초안 — 담당자 배정 전 참고용 |
