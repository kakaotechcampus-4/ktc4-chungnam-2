# 여행 공동 지도 — 최종 기획안

작성 기준: 현재 프로토타입(`prototype.html`)에 실제로 구현된 동작
최종 갱신: 2026-08-22

> **핀 종류·확정 리스트 재정의 반영 중.** 이 문서(3·4·5-2·5-3·5-5-1·6·7·8·9·10절)는 새 모델(확정은 사용자가 직접 관리, 좋음 과반 조건 없음 / 대안 후보는 점선 핀으로 지도에 뜨되 나에게만 보임)로 갱신됐지만, `prototype.html`은 아직 이전 모델(좋음 과반+반대 0 자동 확정, 대안 후보는 카드로만 노출)로 동작한다. 14절 참고.

---

## 1. 개요

### 한 줄

친구 3~5명이 하나의 공동 지도에 핀을 찍어 여행 장소를 정하고, 의견이 갈리면 반대 사유를 반영한 대안 핀을 받아 합의에 이르는 서비스.

### 문제

장소를 모으는 건 지도로 해결된다. 문제는 그다음이다.
누가 "○○ 별로야"라고 말한 뒤 다음 후보를 아무도 내지 않아 결정이 멈춘다.
멈춘 대화는 잡담에 묻히고, 며칠 뒤 "그래서 어디로 정했지"가 반복된다.

여기가 병목이고, AI는 여기에만 쓴다.

### 타깃

방학마다 3~5명이 함께 여행을 가는 대학생.
지금은 카톡에 링크를 공유하고, 구글맵 저장목록이나 스프레드시트에 다시 옮기고, 의견은 따로 물어보는 식으로 버틴다.

### 핵심 가치

하나의 지도에 핀을 찍으면 핀별 의견이 한눈에 보이고,
의견이 갈리면 반대 사유를 반영한 대안 핀이 올라오며,
사용자가 확정한 곳이 확정 리스트에 남는다.

---

## 2. 가장 중요한 설계 결정 — 제약을 두 종류로 나눈다

이 서비스의 나머지 설계가 전부 여기서 파생된다.

|           | 실격 (hard)                                                | 선호 (soft)                       |
| --------- | ---------------------------------------------------------- | --------------------------------- |
| 무엇      | 못 먹는 재료·알러지, 수용 인원, 예산 상한, 반경, 영업 여부 | 분위기, 지역색, 한적함            |
| 누가 판정 | 코드. 위반하면 후보 목록에서 제거                          | 모델. 통과한 후보의 순위에만 반영 |
| 틀렸을 때 | 사용자가 싫다고 말한 것을 추천하는 사고                    | 취향이 안 맞는 정도               |
| UI 표기   | 「꼭 지켜야 해요」                                         | 「있으면 좋아요」                 |

### 왜 이렇게 나눴나

1차 실측 20건에서 검증 가능한 제약(재료·인원·가격)이 걸린 6건은 0/6 전부 실패했다.
실패 6건 중 4건이 실격 조건 위반이었고, 그중 둘은 "해산물 못 먹음"에 재첩국과 초밥을 추천한 경우였다.

프롬프트로 고칠 문제가 아니라고 판단했다. `price <= 15000`은 틀릴 방법이 없지만, 생성은 아무리 좋아져도 확률이라 0%가 되지 않는다. 알러지는 한 번 틀리면 안 되는 조건이다.

---

## 3. 에이전트 루프

프로토타입 하단 계기판에서 각 단계가 실제로 도는 것을 볼 수 있다.

|       | 단계      | 담당                    | 하는 일                                                                                   |
| ----- | --------- | ----------------------- | ----------------------------------------------------------------------------------------- |
| ⓪     | 준비      | 코드                    | 카테고리별 핀 수가 구성원 절반 이상인지 판정                                              |
| ▶     | 실행 요청 | 사람                    | 「추천 받기」를 누른다                                                                    |
| ①     | 인식      | 코드                    | 핀·반응·사유·제외목록·기준 좌표 수집                                                      |
| ②     | 계획      | 모델                    | 사유를 실격/선호로 분류하고 규칙으로 작성                                                 |
| 🙋    | 근거 확인 | 사람                    | 근거 리스트를 보고 −로 빼거나 ＋로 추가                                                   |
| ③-a-1 | 라벨링    | 모델 (캐시 미스 시에만) | 후보의 재료·붐빔처럼 장소 API에 없는 사실을 판단해 캐시에 저장                            |
| ③-a-2 | 실격 필터 | 코드                    | 캐시된 라벨과 값 필드로 규칙 위반 후보 제거                                               |
| ③-b   | 선호 순위 | 모델                    | 통과 후보만 점수를 매겨 상위 3곳 선정                                                     |
| ④     | 반영      | 코드                    | 근거·체크리스트를 붙여 **점선 핀으로 지도에 띄운다 — 이 시점엔 요청한 사람에게만 보인다** |
| ⑤     | 게시      | 사람                    | 마음에 드는 후보를 「지도에 올리기」 — 이때부터 다른 구성원에게도 보인다                  |

요청마다 반드시 도는 모델 호출은 ②와 ③-b 두 곳이다. ③-a-1은 후보가 캐시에 없을 때만 도는 별도 경로라 이 둘과 성격이 다르다 — 자세한 흐름은 5-8 참고.
사람이 개입하는 곳은 ▶·🙋·⑤, 그리고 확정 리스트에 넣는 행동까지 네 곳.

AI 루프는 ④에서 끝난다. ④에서 점선 핀은 이미 지도에 뜨지만 요청한 사람에게만 보인다. 이걸 다른 구성원에게도 보이게 할지(⑤)는 전적으로 요청한 사람의 판단이다.
가드레일 1(AI는 지도를 바꾸지 않는다)이 여기서 더 엄격해진 것 — AI 결과가 뜨는 범위를 처음엔 한 사람으로 좁혀두고, 팀 전체에 보이는 것 자체를 사람이 결정하게 했다.

### 챗봇과의 차이

|           | 챗봇                | 이 서비스                                   |
| --------- | ------------------- | ------------------------------------------- |
| 입력      | 사람이 쓴 문장 하나 | 시스템이 읽은 지도 상태 (①)                 |
| 출력      | 스크롤 위 텍스트    | 지도에 남는 핀·확정 (④ 이후, 전체 공개는 ⑤) |
| 다음 실행 | 처음부터 다시 설명  | 앞 실행 결과를 이어받음                     |

지도에 오른 후보와 「다시 추천 받기」로 교체되며 빠진 후보 모두 제외목록에 들어가 ①의 입력이 되어 루프가 닫힌다.
실측에서 챗봇이 서로 다른 입력 네 건에 같은 가게를 낸 것, 숙소 위치를 몰라 거리 조건을 포기한 것이 정확히 이 두 가지가 없어서 생긴 실패다.

---

## 4. 화면 구조

> **2026-09-02 갱신.** 아래는 데스크톱 레일+패널 구조였으나, README(팀 저장소 정본)가 이미 **탭 3개 구조로 확정**해뒀다는 걸 뒤늦게 확인해 반영했다. GitHub 이슈명("2. 지도 탭", "3. AI 추천 탭", "4. 리스트 탭")도 처음부터 이 구조를 전제하고 있었다.

모바일 우선 웹 서비스. 하단(또는 상단) 탭 3개 + 전체 지도.

| 탭 | 내용 |
| --- | --- |
| 지도 (기본) | 분류·종류 이중 필터 + 전체 지도. 장소 검색·핀찍기·핀 목록·핀 상세는 이 탭 위에 바텀시트로 뜬다(GitHub #16~21 참고) |
| AI 추천 | 지도가 없다. 근거 확인 화면 → 결과 카드 3곳. 대안의 위치 자체는 지도 탭에서 점선 핀으로 본다 |
| 리스트(확정) | 왼쪽 핀 목록, 오른쪽 최종 코스. 오른쪽으로 드래그하면 그 자체가 확정이고 드롭한 자리가 방문 순서(5-10 동선과의 관계는 결정 이슈 "visit_order 수동 정렬 허용 여부" 참고) |

핀 모양은 3종(확정=채운 핀, 일반=실선, AI 추천=점선, 아래 표 참고)으로 구분하고, **색은 별도로 "누가 찍었는지"를 나타낸다.** 단 확정 핀과 AI 추천 핀은 고정색을 써서 제안자 색을 버린다 — 색 팔레트·배정 방식 자체는 아직 결정 이슈로 열려 있다.

지도 상태는 **실시간으로 모든 구성원에게 동기화된다.** 누가 핀을 찍거나 반응을 남기거나 확정 리스트를 바꾸면, 새로고침 없이 다른 구성원 화면에도 곧바로 반영된다.

### 지도 위 이중 필터

- 분류: 전체 / 음식점 / 카페 / 숙소 / 관광지 (각각 개수 표시)
- 종류: 확정 / 일반 / AI 추천 (각각 개수 표시)

두 필터가 곱해져서 적용되고, 목록 패널도 같이 걸러진다.

> 이전엔 반응 집계로 계산한 4상태(합의/조율 필요/제외/미확인)를 필터로 썼다. 지금은 5-2에서 정의한 3종류로 바뀌었다 — 반응(♥/△/🚫) 자체는 그대로 남지만, 그 반응이 핀의 종류를 자동으로 바꾸지는 않는다.

### 핀 모양 3종

지도는 이미 색을 쓰고 있으므로 종류는 색이 아니라 모양으로 구분한다.

| 종류    | 모양      | 뜻                                                                                           |
| ------- | --------- | -------------------------------------------------------------------------------------------- |
| 확정    | 채운 핀 ♥ | 사용자가 확정 리스트에 넣은 핀. 원래 종류(일반/AI 추천)보다 우선한다                         |
| 일반    | 실선 핀   | 사람이 직접 찍은 핀. 처음 찍히는 순간부터 이 모양이다                                        |
| AI 추천 | 점선 핀   | AI가 추천한 핀. 「지도에 올리기」 전엔 요청한 사람에게만, 이후엔 모두에게 이 모양으로 보인다 |

반대를 많이 받아도 핀의 모양은 바뀌지 않는다. 일반 핀은 이미 지도 위에 실선으로 모두에게 보이고, 반응(♥/△/🚫) 자체가 의사표시로 뜨기 때문에 흐리게 만들 필요가 없다.
다만 AI 추천 과정에서 이미 제안했거나 「관심 없음」으로 접은 장소는 다음 검색에서 후보로 다시 뜨지 않는다(5-6) — 이건 화면에 보이는 핀의 모양이 아니라 코드가 들고 있는 이력이다.

---

## 5. 규칙 명세

### 5-1. 반응과 사유

| 반응      | 표기 | 사유                                 |
| --------- | ---- | ------------------------------------ |
| 좋음      | ♥    | 자유서술 (선택) — 순위 참고로만 쓰임 |
| 조율 필요 | △    | 자유서술 (선택) — 순위 참고로만 쓰임 |
| 반대      | 🚫   | 카테고리별 칩 또는 자유서술 (필수)   |
| 미확인    | ?    | 아직 답하지 않음                     |

사유 없는 반대는 등록 버튼이 비활성화되어 입구에서 차단된다.
좋음·조율 사유는 후보를 거르지 않는다. 좋다는 이유로 후보가 줄어드는 일은 없다.

### 5-2. 핀 종류 (코드가 표시, 사람이 바꿈)

핀의 종류는 3가지뿐이다. 투표를 집계해서 계산하지 않는다.

| 종류    | 언제 이 종류가 되나                                         |
| ------- | ----------------------------------------------------------- |
| 일반    | 사람이 직접 찍은 순간부터                                   |
| AI 추천 | AI 추천으로 생겨난 순간부터                                 |
| 확정    | 사용자가 확정 리스트에 넣는 순간부터 — 원래 종류를 덮어쓴다 |

반응(♥/△/🚫)은 그대로 남아 각 핀에 쌓이지만, 이 반응들이 핀의 종류를 자동으로 바꾸지는 않는다. 종류를 바꾸는 유일한 방법은 사용자가 확정 리스트에 넣거나 빼는 것이다.

### 5-3. 확정 리스트

확정 리스트는 집계가 아니라 사용자가 직접 관리하는 목록이다. 좋음 과반, 반대 0 같은 투표 조건이 없다. 전원 합의도 필요 없다.

핀을 확정 리스트에 넣는 것도 빼는 것도 사용자가 한다. 카테고리 칩으로 걸러 볼 수 있고, 원래 종류가 일반이든 AI 추천이든 상관없이 넣을 수 있다.

> 구성원 누구나 넣고 뺄 수 있다 — 지도를 만든 사람으로 한정하지 않는다.

### 5-4. 추천 준비 임계값

**트리거는 수동이다.** 조건이 충족돼도 추천이 저절로 시작되지 않는다 — 사람이 「추천 받기」를 눌러야 한다.

버튼이 눌릴 수 있는 상태(활성화)가 되는 조건은 이렇다.
카테고리별로 **의견(♥/△/🚫)이 하나라도 남겨진 핀**의 수가 구성원 절반(`ceil(N/2)`, 4명 기준 2개) 이상이면 그 카테고리 추천 버튼이 활성화된다.

### 5-5. 근거 리스트

「추천 받기」를 누르면 바로 실행되지 않고 근거 확인 팝업이 먼저 뜬다. **근거는 구성원별로 한 줄씩 따로 뜬다** — 같은 칩을 여러 명이 골라도 하나로 합치지 않는다.

```
음식점 대안 — 이 근거로 찾을게요
────────────────────────────────
[꼭]   A: 매워요        매운맛 전문 제외        [−]
[꼭]   B: 매워요        매운맛 전문 제외        [−]
[꼭]   C: 비싸요        1인 15,000원 이하       [−]
[참고] A: 국물 진하고…  좋음 사유 — 순위 참고    [−]
[＋]  조건을 더 적을게요
────────────────────────────────
지금 근거 4개 · 그중 후보를 거르는 조건 3개
[이대로 추천 받기]  [다시 추천 받기]
```

| 배지 | 출처                                     | 역할                 |
| ---- | ---------------------------------------- | -------------------- |
| 꼭   | 반대 + 실격 칩                           | 코드가 후보에서 제거 |
| 선호 | 반대 + 선호 칩                           | 모델이 순위에만 반영 |
| 참고 | 좋음·조율 사유, 직접 추가한 조건, 피드백 | 후보를 거르지 않음   |

- `−`로 뺀 근거는 실제로 필터에서 빠진다
- `＋`로 자유서술 조건을 추가할 수 있다
- 「다시 추천 받기」는 지금 떠 있는 대안을 제외 목록에 넣고 새로 찾는다
- **근거는 각자 자기가 쓴 것만 −로 뺄 수 있다.** 한 줄에 한 사람 것만 있으므로, 남이 쓴 근거를 대신 지우는 경우 자체가 없다 — A가 자기 줄(A: 매워요)을 빼도 B의 줄(B: 매워요)은 그대로 남는다.
- **같은 조건이라도 활성 상태인 줄이 하나라도 있으면 그 조건은 계속 필터에 적용된다.** 위 예시에서 A가 자기 줄을 빼도 B의 "매워요"가 남아 있으니 매운맛 전문 제외는 계속 걸린다. 그 조건을 걸어둔 사람 전원이 각자 자기 줄을 빼야 실제로 필터에서 사라진다.

### 5-5-1. 대안 후보는 점선 핀으로 뜨되, 나에게만 보인다

「이대로 추천 받기」로 나온 3곳은 **점선 핀으로 지도 위에 바로 뜬다.** 다만 요청한 사람에게만 보이고, 다른 구성원 화면에는 나타나지 않는다.

각 후보에는 버튼이 하나뿐이다.

| 버튼          | 하는 일                                    |
| ------------- | ------------------------------------------ |
| 지도에 올리기 | 그 점선 핀을 다른 구성원에게도 보이게 한다 |

「지도에 올리기」를 누르는 순간 그 핀은 근거·출처를 그대로 지닌 채 다른 구성원 화면에도 나타난다. 종류는 여전히 `AI 추천`이고(5-2), 원한다면 사용자가 확정 리스트에 넣을 수 있다(5-3) — 자동으로 들어가지 않는다.

올리지 않고 놔둔 나머지 후보는 계속 나에게만 보이는 상태로 남고, 「다시 추천 받기」를 누르면 그때 제외 목록에 들어가며 교체된다.

**이게 왜 필요한가:** AI가 3곳을 추천하는 순간 바로 팀 전체 화면에 뜨면, 요청한 사람이 걸러볼 틈도 없이 팀원들이 이상한 후보부터 보게 된다. 지도에 뜨는 범위를 처음엔 한 사람으로 좁혀서, "AI가 제안 → 한 사람이 훑어봄 → 괜찮은 것만 팀에 공개"로 만들었다.

### 5-6. 실격 필터 순서 (코드)

```
1. 카테고리 후보 풀
2. 반경 밖 제거 (5-6-1의 유효 지역 기준 — 겹치는 원의 교집합, 안 겹치는 무리끼리는 합집합)
3. 영업 종료 제거
4. 활성 실격 조건별 제거 (근거 리스트에서 −로 뺀 것은 적용하지 않음)
5. 이미 제안·거절된 곳 제거
6. 선호 순위 정렬 후 상위 3곳
```

실행 결과는 계기판의 깔때기 표에 그대로 표시된다. 초기 상태(반경 15분, 반대 사유 = 매워요·비싸요) 실행 예시:

```
음식점 후보          18곳
└ 반경 15분 밖        −3
└ 영업 종료           −1
└ 매운맛 전문         −3
└ 예산 초과           −5
└ 이미 제안·거절됨    −1
남은 후보              5곳  → 상위 3곳 제시
```

이 숫자는 하드코딩이 아니라 실행 시점에 계산된 값이다. 반경을 8분으로 좁히면 숫자가 바뀌고, 근거에서 「매워요」를 빼면 −3이 사라진다.

### 5-6-1. 검색 범위 계산 (반경)

반경은 한 점에서 잰 거리가 아니라 **핀·사유마다 그린 원들을 묶고 겹친 영역**이다. 겹치는 원끼리는 교집합(더 엄격한 쪽), 안 겹치는 무리끼리는 합집합으로 각각 인정한다.

**기본값 원:** 그 지도에 찍힌 핀 전부(카테고리·상태 무관)를 각각 중심으로 기본 반경(도보 15분에 해당하는 거리) 원을 그린다. 카테고리를 가리지 않는 이유는 "여행지를 벗어나면 안 된다"가 지리적 제약이라 음식점을 추천받을 때도 숙소·관광지 핀과의 거리를 함께 봐야 하기 때문이다.

**사유가 있으면 그 기준점의 원을 대체한다:** "숙소 기준 도보 20분"처럼 기준점과 거리가 담긴 자유서술을 모델이 읽고, 그 기준점의 원을 기본값 대신 그 값으로 바꾼다. 같은 기준점을 두고 여러 명이 서로 다른 값을 남기면 더 엄격한(짧은) 쪽이 이긴다 — 실격 조건과 같은 원칙이다. 근거 리스트에도 구성원별로 한 줄씩 뜨고(5-5), 자기 사유만 뺄 수 있다.

**서로 겹치지 않는 원은 각각 다른 지역으로 인정한다 (교집합이 아니라 합집합):** 여행이 여러 날, 여러 동네에 걸쳐 있으면 원들이 자연스럽게 두세 무리로 나뉜다. 이걸 억지로 하나의 교집합으로 묶으면 존재할 수 없는 지역이 나온다. 그래서 겹치는 원들끼리는 무리를 짓고(교집합), 무리와 무리는 합쳐서(합집합) 전부 유효한 검색 범위로 인정한다.

```
A: "해운대 기준 도보 5분"  ─┐
                          ├─ 서로 안 겹침 → 두 지역 다 인정
B: "광안리 기준 도보 1분"  ─┘

검색 범위 = 해운대 반경 5분 지역  OR  광안리 반경 1분 지역
```

**단, 사람이 쓴 사유끼리 안 겹칠 때만 먼저 확인받는다.** 아무도 사유를 안 썼는데 핀들이 그냥 멀리 떨어져 있는 경우(예: 첫날 숙소와 둘째 날 숙소)는 조용히 각각의 지역으로 인정한다 — 사람이 뭘 주장한 게 아니라 원래 지도에 있던 사실이라 되물을 게 없다. 반면 **사람이 명시적으로 쓴 반경 사유가 다른 무언가와 안 겹치면**, 근거 확인 팝업에 확인창을 띄운다.

```
⚠ 두 조건을 동시에 만족하는 곳이 없어요

A: 해운대 기준 도보 5분
B: 광안리 기준 도보 1분

[ 두 지역 각각 찾기 ]   [ 근거를 다시 볼게요 ]
```

「두 지역 각각 찾기」를 누르면 그 조합은 합집합으로 확정된다. 이후 같은 두 사유가 그대로 남아 있는 한 다시 묻지 않는다 — 둘 중 하나가 수정되거나 새 사유가 추가돼 겹침 여부가 바뀔 때만 다시 확인한다.

**결과 3곳을 뽑을 때 지역별로 안배한다:** 유효한 지역이 여럿이면(위 예시처럼 해운대·광안리 2곳) 각 지역에서 순위 1위를 먼저 확정하고, 남는 자리를 전체 순위로 채운다. 지역이 3개를 넘으면 전부 안배할 수 없으므로, **반대·조율 사유가 더 많이 붙어 의견이 많이 갈린 지역부터 우선한다** — 애초에 추천을 요청하게 만든 병목이 의견이 갈린 곳이기 때문이다. 결과 카드에는 어느 지역 기준으로 뽑혔는지 태그를 붙인다(가드레일 5, 출처 표시).

**넓히기는 기본값 원만 건드린다:** 결과가 없어서 반경을 넓힐 때(6절의 "반경 넓히기") 커지는 건 기본값 원뿐이다. 사람이 사유로 명시한 원(예: 숙소 기준 20분)은 자동으로 넓히지 않는다 — 가드레일 4, AI가 사람이 정한 제약을 임의로 완화하지 않는다는 원칙과 같다.

거리 계산은 직선거리다(13절 PostGIS 참고). 화면엔 "도보 N분"으로 근사 표기한다.

### 5-7. 카테고리별 실격 조건

| 카테고리 | 실격 (코드가 거름)                                                                | 선호 (순위에만)                           |
| -------- | --------------------------------------------------------------------------------- | ----------------------------------------- |
| 음식점   | 1인 15,000원 이하 · 매운맛 전문 제외 · 기름진 메뉴 제외 · 갑각류 제외 · 도보 15분 | 대기 짧은 곳                              |
| 카페     | 음료 10,000원 이하 · 붐비는 대형 카페 제외 · 도보 15분                            | 조용한 곳 · 좌석 편한 곳 · 지역색         |
| 숙소     | 1박 100,000원 이하 · 3인 이상 수용 · 도보 15분                                    | 조용한 위치 · 감성적 공간 · 최근 리모델링 |
| 관광지   | 입장료 15,000원 이하 · 붐비는 명소 제외 · 도보 15분                               | 현지 분위기 · 볼거리 · 실내 대안          |

### 5-8. 장소 라벨링 (캐시)

③-a-2가 값을 비교하려면 후보마다 가격·재료·붐빔 같은 값이 미리 구조화되어 있어야 한다. 장소 API에는 "갑각류 포함 여부" 같은 필드가 없으므로, 그 값을 채우는 경로가 따로 필요하다.

**트리거:** 사용자가 핀을 찍는 순간이 아니라, AI가 추천을 위해 반경 안에서 후보를 검색하는 순간이다.

```
반경 검색으로 후보 N곳 확보
        ↓
   후보마다 장소 단위 캐시 조회
        ↓                    ↓
     라벨 있음              라벨 없음
        ↓                    ↓
     그대로 씀        모델이 그 자리에서 라벨링 → 캐시에 저장
                          (판단 불확실하면 "모름"으로 저장)
        ↓                    ↓
              ③-a-2 실격 필터로
```

- 캐시는 **장소 단위**지 지도 단위가 아니다. 같은 가게가 다른 팀의 다른 지도에서 후보로 잡혀도 이미 채워진 라벨을 그대로 재사용한다.
- 지도가 쌓일수록, 특히 같은 지역 안에서는 새로 라벨링해야 하는 후보 비율이 줄어든다. 유명한 곳부터 먼저 채워지기 때문이다. 44.txt의 "실격 판정 사전"이 실제로 쌓이는 방식이 이것이다.
- 라벨링 대상은 대안 핀으로 뽑힌 곳만이 아니라 **검색된 후보 전체**다. 필터에서 떨어진 곳도 라벨은 캐시에 남아 다음 검색에 재사용된다.
- 판단이 "모름"으로 남은 후보는 가드레일 8에 따라 자동 확정 없이 "메뉴 확인 필요"로 표시한다.
- 사람이 직접 찍은 핀(검색·링크·길게 누르기)은 이 경로를 타지 않는다. 그 핀의 상태는 사람의 ♥/△/🚫 반응으로 정해지므로 라벨링이 필요 없다.

### 5-9. 카톡 대화 내보내기로 일괄 핀 찍기

여행 얘기를 나눈 단톡방을 다시 뒤져 하나씩 옮겨 찍을 필요가 없도록, 카톡 내보내기 텍스트를 통째로 붙여넣으면 그 안의 구글맵 링크를 한꺼번에 핀 후보로 만든다. **① 구글맵 링크 붙여넣기의 대량 버전**이지 새로운 입력 경로가 아니다.

```
카톡 내보내기 텍스트 붙여넣기
        ↓
   코드가 정규식으로 구글맵 링크만 스캔       ← 모델 호출 없음
        ↓
   링크마다 기존 ① 경로 그대로 실행 (좌표·카테고리 조회)
        ↓
   이미 지도에 있는 곳은 목록에서 빼고 표시
        ↓
   "링크 N개 찾았어요" 목록 확인 → 사람이 훑어보고 한꺼번에 핀 찍기
```

- **텍스트 내용을 해석하지 않는다.** "이 문장이 장소 제안인지 잡담인지" 판단은 하지 않는다 — 이건 44.txt에서 이미 "지금 모델로는 안정적으로 못 한다"고 판단한 일이다. 링크가 있으면 뽑고, 없으면 그 줄은 그냥 무시한다. 그래서 모델 호출이 늘지 않는다 — 여전히 2곳뿐이다.
- **구글맵 링크가 아닌 것은 안 잡는다.** 네이버맵·카카오맵 링크나 "돈까스집 어때" 같은 순수 텍스트 언급은 잡지 않는다. 잡고 싶으면 ②(이름 검색)로 직접 찾아 찍어야 한다.
- **대화 내용은 저장하지도, 서버로 보내지도 않는다.** 정규식 스캔은 브라우저 안에서 끝내고 추출된 링크만 다음 단계로 넘긴다. 참여자 이름·시간·잡담 내용은 화면에도, 어디에도 남지 않는다 — 12절 개인정보 최소 수집 원칙과 같다.
- **찾은 링크가 0개면** "구글맵 링크를 찾지 못했어요. 링크가 포함된 대화 구간을 붙여넣어 주세요"로 빈 상태를 보여준다.
- 이미 지도에 있는 장소(같은 곳)는 중복으로 다시 찍지 않는다 — 가드레일 6과 같은 원칙.

### 5-10. 확정 리스트 — 효율적인 동선 보기

확정 리스트에 핀이 2개 이상이면 "동선 보기" 토글이 뜬다. **순서만 제안하고 시간은 배정하지 않는다.** 몇 시에 어디를 가라는 건 전체 일정 자동생성이고, 이건 스코프 밖이다(11절).

```
확정 핀들을 5-6-1과 같은 방식으로 지역별로 묶는다
        ↓
   무리마다 따로 경로 계산 (묶지 않은 무리끼리는 동선도 안 묶는다)
        ↓
   그 무리에 확정된 숙소 핀이 있으면 출발·도착 기준점으로 삼는다
        ↓
   나머지 확정 핀을 가장 가까운 순으로 도는 경로 계산 (최근접 이웃 + 필요시 순서 재배치)
        ↓
   지도에 선과 방문 순서(①②③)로 표시
```

- **지역이 여러 개면 동선도 여러 개다.** 첫날 해운대에서 확정된 3곳, 둘째 날 광안리에서 확정된 2곳이 있으면 "해운대 동선", "광안리 동선"을 따로 보여준다. 8km 떨어진 곳을 하나의 경로로 억지로 묶지 않는다 — 5-6-1과 같은 이유다.
- **순수 계산이다. 모델을 쓰지 않는다.** 좌표 몇 개로 총 이동 거리가 최소가 되는 순서를 찾는 문제라 코드로 끝난다. 모델 호출은 여전히 2곳뿐이다.
- **영업시간·식사시간 같은 시간 요소는 고려하지 않는다.** 순서만 제안한다.
- **다시 계산 받기 버튼이 필요 없다.** 사람의 합의를 흔드는 게 아니라 이미 확정된 것을 보기 좋게 배열하는 것뿐이라, 확정 리스트가 바뀌면 그냥 즉시 다시 그린다 — AI 추천처럼 사람이 눌러야 재계산되는 것과 다르다.
- 거리 계산은 직선거리다(13절 PostGIS 참고). 화면엔 "약 OO분"으로 근사 표기한다.

---

## 6. 핵심 시나리오

로그인부터 확정까지 끊기지 않고 이어진다.

```
초대 링크
  → 소셜 로그인 1탭 (표시 이름·식별자만)
  → 지도 진입
  → 구글맵 링크 붙여넣기 → 카테고리 확인 → 핀 찍기
  → 핀 탭 → 반대 + 사유 칩
  → 음식점 핀 2개 도달, 추천 버튼 열림
  → 「추천 받기」 → 근거 확인 팝업 → 근거 수정 → 이대로 추천 받기
  → 8단계 진행 표시
  → 대안 후보 3곳, 점선 핀으로 뜨지만 나에게만 보임 (이유 · 조건 체크 · 구성원 충족 · 출처)
  → 「지도에 올리기」 → 이제 모든 구성원에게 보임
  → 다른 구성원들이 ♥/△/🚫로 반응
  → 마음에 드는 핀을 누군가 확정 리스트에 추가 → 되돌리려면 리스트에서 뺌
  → 마음에 안 들면 「근거 고쳐서 다시 추천 받기」로 재진입 (3회 상한)
```

### 함께 만든 화면

행복한 경로만 만들지 않았다.

| 상황                  | 화면                                                                 |
| --------------------- | -------------------------------------------------------------------- |
| 핀이 하나도 없음      | "아직 아무도 핀을 찍지 않았어요" + 입력 경로 3개 안내                |
| 필터에 걸리는 핀 없음 | "필터에 걸리는 핀이 없어요"                                          |
| 추천 준비 미달        | "핀 1/2 · 1개 더 필요"                                               |
| 처리 중               | 8단계 중 현재 단계 실시간 표시                                       |
| 결과 0개              | 어느 조건이 몇 곳을 걸렀는지 + 반경 넓히기 / 근거 고치기 / 직접 찍기 |
| 추천 실패             | "조건이 까다로워서가 아니에요" + "남긴 것은 그대로 있어요"           |
| 재시도 3회 초과       | "3번까지만 찾습니다"                                                 |
| 확정 분류에 항목 없음 | "이 분류에는 확정된 곳이 없어요"                                     |

결과가 0개면 억지로 채우지 않고 0개로 표시한다. 조건을 푸는 것은 사람이 한다.

---

## 7. 가드레일

1. AI는 지도를 바꾸지 않는다. 대안 3곳은 점선 핀으로 지도 위에 바로 뜨지만 요청한 사람에게만 보이고, 그 사람이 「지도에 올리기」를 눌러야 다른 구성원에게도 보인다.
2. 지도 API에서 조회되지 않는 장소는 핀으로 만들지 않는다. 결과가 0개면 0개로 표시한다.
3. 사유 없는 반대는 등록되지 않는다. 해석 불가한 사유는 선택지로 되묻는다.
4. 제약을 AI가 임의로 완화하지 않는다. 반경을 넓히는 것은 사람이 한다.
5. 대안 핀에는 추천 이유, 조건별 충족 체크, 구성원 충족 집계, 지도 출처를 붙인다. 근거 없는 한 줄 추천 금지.
6. 이미 제안했거나 거절된 장소는 후보에서 제외한다. 몇 곳을 뺐는지 화면에 밝힌다.
7. 실격 조건은 코드가 거른다. 모델은 실격 통과분의 순위만 매긴다.
8. 재료는 카테고리가 아니라 대표 메뉴로 판정한다. 판정이 불확실하면 "메뉴 확인 필요"로 표시하고 확정하지 않는다.
9. 한 명이라도 실격이면 후보에서 내린다. 다수 중 한 사람을 조용히 배제하지 않는다.
10. 충돌 선언은 최후에만. 불가능하다고 말하기 전에 둘 다 만족하는 범주를 먼저 탐색하고, 무엇을 시도했는지 화면에 적는다.

5~10은 실측 실패에서 나온 규칙이다.

### 자동 업데이트가 반응을 흔들지 않게

지도에 올라간 대안은 재계산으로 교체하지 않는다. 「지도에 올리기」를 거친 순간(5-5-1) 추천 로직이 다루는 대상에서 완전히 벗어나므로, 이 규칙은 특별한 예외 처리 없이 구조적으로 지켜진다. 재계산이 갈아끼우는 대상은 아직 나에게만 보이는, 반응이 있을 수 없는 비공개 후보뿐이다.

---

## 8. AI / 규칙 분리

### 모델이 하는 일

**요청마다 반드시 도는 것 (2곳)**

- 사유를 제약으로 구조화하고 실격/선호로 분류 (②)
- 실격 통과분의 선호 순위 매기기 (③-b)

**후보가 캐시에 없을 때만 도는 것 (③-a-1)**

- 후보 장소의 재료·붐빔처럼 장소 API에 없는 사실을 라벨링해 캐시에 저장. 불확실하면 "모름"으로 남기고 확정하지 않음 (5-8 참고)

### 코드가 하는 일

로그인·계정 · 링크 파싱(카톡 대화 내보내기 일괄 파싱 포함, 5-9) · 좌표 조회 · 카테고리 분류 · 추천 준비 판정 · 라벨 캐시 조회 · 실격 필터 · 핀 종류 관리(일반/AI 추천/확정) · 반경 필터 · 영업 여부 확인 · 중복·거절 이력 제외 · 확정 리스트 관리(사용자의 추가·제외 반영, 투표 집계 아님) · 근거 리스트 조립 · 동선 계산(5-10)

새 기능을 추가할 때 먼저 물을 것: "이거 코드로 되나?"
값이 있는 조건이면 답은 거의 항상 "된다".

---

## 9. 용어 고정

UI·코드·문서에서 동일하게 쓴다.

| 구분                                           | 용어                                                                              |
| ---------------------------------------------- | --------------------------------------------------------------------------------- |
| 반응                                           | ♥ 좋음 / △ 조율 필요 / 🚫 반대 / ? 미확인                                         |
| 핀 종류                                        | 확정 / 일반 / AI 추천 (이 3개 외 종류를 만들지 않는다. 확정이 나머지 둘보다 우선) |
| AI 추천 핀 (지도에 오르기 전)                  | 점선 핀으로 지도에 뜨되, 요청한 사람에게만 보임                                   |
| AI 추천 핀 (지도에 오른 뒤)                    | 다른 구성원에게도 보임. 종류는 여전히 AI 추천                                     |
| 비공개 상태를 다른 구성원에게 보이게 하는 행동 | 지도에 올리기                                                                     |
| 카테고리                                       | 음식점 / 카페 / 숙소 / 관광지                                                     |
| 제약                                           | 실격 (UI: 꼭 지켜야 해요) / 선호 (UI: 있으면 좋아요)                              |
| 사용자가 확정한 핀 모음                        | 확정 리스트                                                                       |

쓰지 않는 말: 장소 카드 · 보드 · 칸반 · 여행방
장소는 언제나 핀, 공간은 언제나 지도.

---

## 10. 성공 지표

| 구분   | 지표                                                       | 목표              |
| ------ | ---------------------------------------------------------- | ----------------- |
| 최우선 | 실격 조건 위반율                                           | 0% (1차 실측 40%) |
| 핵심   | 조율 필요 → 확정 전환율                                    | 10건 중 6건       |
| 보조   | 대안 게시율 (비공개 후보 중 「지도에 올리기」까지 간 비율) | 50% 이상          |
| 보조   | 대안 채택률 (지도에 오른 대안이 확정까지 간 비율)          | 60% 이상          |
| 보조   | 24시간 내 결정 비율                                        | 70% 이상          |
| 보조   | 진입 전환율 (링크 클릭 → 첫 반응)                          | 70% 이상          |
| 보조   | 반대 대비 사유 기록률                                      | 80% 이상          |
| 보조   | 같은 장소 반복 추천 비율                                   | 20% 이하          |

---

## 11. 스코프

### 범위 안

공동 지도 · 초대 링크 · 소셜 로그인 · 구글맵 링크 파싱 · 핀 반응과 사유 · 실격 필터 · 대안 추천 · 확정 리스트
숙소를 후보 핀으로 올려 함께 고르는 것도 범위 안이다.
확정 리스트의 **동선(방문 순서) 보기**도 범위 안이다(5-10) — 순서만 제안하고 시간은 배정하지 않는다.

### v2로 이동

**카톡 대화 내보내기 일괄 파싱(5-9)** — 2026-09-02, 팀 결정으로 v1에서 v2로 이동. 아래 "범위 밖"과는 다르다 — 요청받아도 안 만드는 게 아니라 v1 이후로 미룬 것. 구성원 강퇴·서비스 피드백도 v2다(`CLAUDE.md` 스코프 경계 참고).

### 범위 밖 (요청받아도 만들지 않는다)

항공·숙박 예약 · 경비 정산 · 자체 채팅 · SNS · 전체 일정 자동생성 · 실시간 위치공유 · 환율·날씨 · AI의 최종 장소 결정 · 인스타·블로그 링크 파싱
※ 동선 "순서"는 범위 안이지만 몇 시에 어디를 가라는 **시간 배정**은 전체 일정 자동생성에 해당해 범위 밖이다.

---

## 12. 개인정보

원칙: 안 받은 정보는 유출되지 않는다. 여행 조율에 필요한 최소치만 받는다.

| 받는 것      | 대응                                                                                   |
| ------------ | -------------------------------------------------------------------------------------- |
| 계정         | 소셜 로그인 제공자 식별자와 표시 이름까지. 이메일·전화번호·생년월일·주소는 받지 않는다 |
| 숙소 핀 위치 | 초대 링크를 가진 사람만 볼 수 있다. 공개 링크나 검색 노출은 만들지 않는다              |
| 못 먹는 재료 | 병명·진단명은 받지 않는다. "갑각류"처럼 재료 이름까지만                                |
| 반응·사유    | 해당 지도의 구성원에게만 보인다                                                        |
| 카톡 대화 내보내기 (5-9) | 대화 내용·참여자 이름은 받지 않는다. 브라우저 안에서 구글맵 링크만 뽑고 나머지 텍스트는 즉시 버린다 |

탈퇴 시 계정과 연결된 사유·반응 기록을 함께 지운다.

---

## 13. 기술 스택

| 구분               | 선택                                            |
| ------------------ | ----------------------------------------------- |
| AI API             | 미정 (#12) — 저비용 모델로 시작해 필요하면 올리는 방향   |
| 지도 API           | **카카오맵**(JS SDK + 카카오 로컬 API) — 2026-09-02 확정. 다만 로컬 API가 가격·평점·영업시간을 안 줘서(13절 "구현 시 주의" 참고), 소스를 하나로 고정하지 않고 자체 DB에 여러 소스를 추상화하는 계층(`docs/architecture.md`)을 둔다 |
| 로그인             | 카카오 소셜로그인                               |
| 웹 프레임워크      | FastAPI + Pydantic V2 + SQLAlchemy              |
| DB                 | PostgreSQL 17 + PostGIS                         |
| 캐시·실시간 동기화 | Redis                                           |
| 프론트엔드         | Vite + React + TypeScript (SPA)                 |
| 배포               | Cloud Run + Cloud SQL (초기 Supabase + Upstash) |
| 디자인 툴          | Figma                                           |
| 협업 툴            | Notion · GitHub · 디스코드                      |

프론트엔드 세부: TanStack Query(서버 상태) · Zustand(UI 상태) · Tailwind CSS + shadcn/ui · 카카오맵 JS SDK(공식 React 래퍼 없음 — 명령형 SDK라 마커 상태는 React 바깥에서 독립 관리, React는 UI 상태만 가짐)

### 구현 시 주의

- 카카오 OAuth 콜백은 FastAPI가 받고 httpOnly 쿠키를 발급한다. 토큰을 프론트에 두지 않는다.
- PostGIS는 직선거리다. 화면에 "도보 N분"으로 쓰면 실제와 어긋나므로 표기를 정직하게 하거나 보정계수를 명시한다.
- 지도 SDK 키는 프론트에 노출되므로 도메인 제한·API 종류 제한·일일 할당량 상한을 건다.
- **카카오 로컬 API는 가격·평점·영업시간 필드가 아예 없다** — 실격조건 상당수를 자체 데이터로 채워야 한다(`docs/architecture.md`의 3층 데이터 모델, #11·#14 참고).

---

## 14. 프로토타입 구현 현황

> 아래는 현재 `prototype.html`이 실제로 하는 동작이다. 이번에 갱신된 핀 종류·확정 리스트·대안 후보 표시 방식(3·4·5-2·5-3·5-5-1절)은 아직 코드에 반영되지 않았다 — 프로토타입은 여전히 좋음 과반+반대 0으로 자동 확정하고, 대안 후보를 카드로만(지도에는 안 보이게) 보여주며, 카드 버튼도 「지도에 올리기」／「관심 없음」 두 개다.

### 실제로 도는 것

- 로그인 게이트 → 지도 진입
- 핀 찍기 (구글맵 링크 경로, 카테고리 지정)
- 반응 4종과 사유 입력 (반대는 카테고리별 칩 6개 + 자유서술)
- 분류 × 상태 이중 필터
- 카테고리별 추천 버튼과 준비 상태 판정
- 근거 확인 팝업 (−로 제거, ＋로 추가, 이대로/다시 추천)
- 실격 필터 (4개 카테고리 각각 다른 판정식, 계산 결과를 깔때기로 표시)
- 대안 후보 카드 (이유 · 조건 체크 · 구성원 충족 집계) — 요청한 사람에게만 노출
- 「지도에 올리기」로 비공개 후보를 공용 핀으로 전환, 「관심 없음」으로 접기
- 지도에 오른 대안은 보통 핀과 동일하게 반응(♥/△/🚫) 받음
- 확정 리스트 집계와 카테고리 필터
- 되돌리기 (♥ 과반 도달 시 토스트로 확정 취소 — 대안·직접 찍은 핀 공통)
- 8단계 루프 진행 표시
- 빈 상태 · 결과 0개 · 추천 실패 · 재시도 3회 상한
- 데모 컨트롤 (반경 3단, 실패 시뮬, 빈 핀 화면, 초기화)

### 하드코딩 상태 (실제 구현 시 API로 교체)

- 장소 후보 풀 38곳 (음식점 18 · 카페 8 · 숙소 6 · 관광지 6)
- 재료·매운맛·붐빔 여부 등 실격 판정에 쓰는 속성값
- 도보 시간
- 구성원 4명 고정
- 모델 호출 2곳 (② 사유 구조화, ③-b 선호 순위)은 규칙 기반으로 시뮬레이션

### 미구현

- 실시간 동기화 (현재는 단일 브라우저 상태 — 회의에서 필수 요구사항으로 확정됨, 4절 참고)
- 이름 검색, 지도 길게 눌러 찍기
- 계정 간 데이터 이어짐
- 카톡 대화 내보내기 일괄 핀 찍기 (5-9) — **v2로 이동** (2026-09-02, 15절 참고). v1 미구현 목록이 아니라 v2 백로그
- 확정 리스트 동선 보기 (5-10, 새로 확정된 기능, v1)

---

## 15. 미결 사항

제출·발표 전에 정해야 하는 것.

> ③-A 담당(코드인가 모델인가) 문제는 해결했다. 라벨링(모델, ③-a-1)과 제거(코드, ③-a-2)로 나누고, 라벨링은 요청마다 도는 게 아니라 후보가 캐시에 없을 때만 도는 별도 경로로 정리했다 — 3절, 5-8, 8절에 반영됨.
>
> 추천 트리거(자동인가 수동인가) 문제도 해결했다. **수동이다.** 카테고리별로 의견이 남겨진 핀이 구성원 절반 이상이 되면 추천 버튼이 활성화되고, 실행은 사람이 버튼을 눌러야 한다 — 5-4에 반영됨.
>
> 핀 종류와 확정 리스트도 재정의했다. 핀 종류는 **확정 / 일반 / AI 추천 3가지뿐**이고, 좋음 과반·반대 0 같은 투표 조건으로 자동 계산되지 않는다. **확정 리스트에 넣는 것은 사용자가 직접 하는 행동**이고 전원 합의가 필요 없다 — 3·4·5-2·5-3·6·7·8·9절에 반영됨. 대안 후보도 카드가 아니라 **점선 핀으로 지도에 바로 뜨되 요청한 사람에게만 보이고**, 「지도에 올리기」로 다른 구성원에게 공개한다. 카드 버튼도 「지도에 올리기」 하나뿐이다 — 5-5-1에 반영됨. `prototype.html`은 아직 이 갱신 전이다(14절 참고).
>
> 이어서 두 가지 더 해결했다. **확정 리스트는 구성원 누구나 넣고 뺄 수 있다** — 지도를 만든 사람으로 한정하지 않는다(15-1). **반대를 많이 받은 핀도 모양을 바꾸지 않는다** — 일반 핀은 이미 실선으로 모두에게 보이고 반응 자체가 의사표시로 뜨므로, 흐리게 만드는 별도 상태가 필요 없다. 다음 추천에서 같은 곳을 다시 안 내미는 것은 화면 표시가 아니라 코드가 들고 있는 이력이다 — 4절에 반영됨.
>
> 권한 모델(15-1)도 세 가지 중 두 가지가 더 풀렸다. **근거 리스트는 자기가 쓴 것만 뺄 수 있다**(5-5). **핀 되돌리기는 구성원 누구나 가능하다** — 반응을 남긴 사람으로 한정하지 않는다. 그리고 **지도 상태는 실시간으로 모든 구성원에게 동기화된다**는 것도 요구사항으로 확정했다 — 4절에 반영됨, 프로토타입은 아직 단일 브라우저 상태로만 동작한다(14절).
>
> 근거 리스트의 형태도 정해졌다. **같은 칩을 여러 명이 골라도 하나로 합치지 않고 구성원별로 한 줄씩 보여준다**(예: A: 매워요 / B: 매워요). 그래서 "자기가 쓴 것만 뺄 수 있다"는 권한 규칙이 예외 없이 적용된다 — 5-5에 반영됨.
>
> 마지막으로 반경 조정도 풀렸다. 한 점 기준 거리가 아니라 **핀·사유마다 그린 원을 묶은 영역**으로 계산한다 — 겹치는 원끼리는 교집합(더 엄격한 쪽), 여행이 여러 날·여러 동네에 걸쳐 있어 안 겹치는 무리끼리는 합집합으로 각각 인정한다. 사람이 쓴 반경 사유끼리 안 겹칠 때만 근거 확인 팝업에서 "두 지역 각각 찾기"로 확인받고, 그 외(핀만 멀리 떨어진 기본값끼리)는 조용히 각각 인정한다. 결과 3곳은 지역별로 안배하고(각 지역 1위 우선, 지역이 3개 넘으면 의견이 더 갈린 지역부터), 카드에 어느 지역 기준인지 태그를 붙인다. 반경 사유도 근거 리스트의 일부라 자기가 쓴 것만 빼는 규칙을 그대로 따르므로, 반경만을 위한 별도 권한 규칙은 필요 없다 — 5-6-1, 15-1에 반영됨. 이걸로 15-1의 네 항목이 전부 정해졌다.
>
> 새 기능도 하나 추가했다. **카톡 대화 내보내기를 붙여넣으면 그 안의 구글맵 링크를 코드가 정규식으로 찾아 한꺼번에 핀으로 찍는다**(5-9). 대화 내용을 모델이 해석하는 건 아니다 — 44.txt에서 이미 "잡담과 제안을 구분 못 해 지금은 안 된다"고 판단한 전체 버전과 달리, 링크만 코드로 뽑는 축소판이라 모델 호출도 2곳 그대로고 대화 내용 자체는 어디에도 저장되지 않는다. (**2026-09-02 — 이후 v2로 이동.** 기능 설계 자체는 이 문단 그대로 유효하지만, v1 범위에서는 뺐다. 11절 "v2로 이동" 참고.)
>
> 15-2(역할 배정)도 채워졌다 — PM 권효정·황준영, 프론트엔드 장현성, 백엔드 김민재, AI·평가셋 김관호, QA 김도윤, 통합 표강준, 발표 김관호.
>
> 확정 리스트에 **동선(방문 순서) 보기**도 추가했다(5-10). 5-6-1의 지역 클러스터링을 그대로 재사용해서, 확정 핀이 여러 지역에 걸쳐 있으면 지역별로 동선을 따로 보여준다. 순수 계산이라 모델을 안 쓰고, 순서만 제안하지 시간은 배정하지 않는다 — "전체 일정 자동생성"은 여전히 스코프 밖이라 11절, CLAUDE.md에 경계를 명시해뒀다.
>
> **2026-09-02 — 팀 저장소 README와 대조해 갱신.** 이 문서(8/22 갱신)와 README가 서로 다른 시점에 각자 갱신되면서 벌어진 차이를 맞췄다. **지도 API는 카카오맵으로 확정**(13절), **화면 구조는 데스크톱 레일+패널이 아니라 탭 3개로 확정**(4절) — 둘 다 README에 이미 답이 있었다. 대조 과정에서 이 문서에도, 기존 GitHub 이슈에도 없던 미결 사항 2개를 새로 찾아 결정 이슈로 등록했다: **추천 제안·거절 이력을 지도 단위로 쌓을지 개인 단위로 쌓을지**, **AI 추천 결과(비공개 후보)를 서버에 영속 저장할지 휘발성으로 둘지.** 둘 다 지금 `docs/data-model.md`가 한쪽 답(지도 단위, 서버 영속)을 전제로 설계돼 있어 먼저 정리가 필요하다.

### 15-1. 권한 모델

네 가지를 물었던 항목이다 — 반경 조정 / 근거 리스트 수정 / 핀 되돌리기 / 확정 리스트 추가·제외. 전부 정해졌다.

| 행동                                      | 권한                                                                                                                                                                                                               |
| ----------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| 확정 리스트에 추가·제외                   | 구성원 누구나 (5-3)                                                                                                                                                                                                |
| 근거 리스트에서 항목 빼기                 | **자기가 쓴 근거만** — 근거는 구성원별로 한 줄씩 따로 있어서(5-5) 남의 줄 자체를 못 건드린다                                                                                                                       |
| 핀 되돌리기 (반응으로 바뀐 상태를 되돌림) | 구성원 누구나 — 반응을 남긴 사람으로 한정하지 않는다                                                                                                                                                               |
| 반경 조정                                 | **별도 권한이 필요 없어졌다.** 공유 슬라이더 같은 게 아니라 핀마다 그린 원의 교집합으로 자동 계산되고(5-6-1), 사람이 남기는 반경 사유도 근거 리스트의 일부라 위 "근거 리스트" 규칙(자기가 쓴 것만)을 그대로 따른다 |

### 15-2. 역할 배정

| 담당 영역              | 담당자         | 비고                                  |
| ---------------------- | -------------- | ------------------------------------- |
| PM · 최종 의사결정권자 | 권효정, 황준영 | 재수렴 회의가 열릴 때 최종 결정       |
| 프론트엔드 · 화면 구현 | 장현성         |                                       |
| 백엔드 · API 연동      | 김민재         | 지도 API, 로그인, DB                  |
| AI 프롬프트 · 평가셋   | 김관호         | 모델 호출 2곳(사유 구조화, 선호 순위) 관리, 평가셋 확장 |
| QA · 평가              | 김도윤         | 매주 평가셋 재실행, 실격 위반율 확인, 공석으로 두지 않는다 |
| 통합                   | 표강준         | 주 1회 정기 머지 담당                 |
| 발표                   | 김관호         |                                       |

### 15-3. 정렬 미팅 요일·시간

제안: 매주 화 13:00, 온라인

---

## 16. 참고

| 문서                 | 내용                               |
| -------------------- | ---------------------------------- |
| `CLAUDE.md`          | 프로젝트 규칙과 가드레일           |
| `prototype.html`     | 동작 프로토타입 (이 기획안의 정본) |
| `wireframe.html`     | 로우파이 와이어프레임              |
| `프로젝트관리안.txt` | 역할·마일스톤·스택                 |
